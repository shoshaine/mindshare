[build]
  command = "npm run build"
  publish = "dist"

# If you want Netlify to run a specific command before publishing (e.g. build assets),
# configure it above. The site will serve from the project root so your existing
# index.html / index2.html and asset folders are published as-is.

# Uncomment and set a specific branch if you want Netlify to auto-deploy from a branch
# [context.production]
#   publish = "."
#   environment = { ENV = "production" }
